version: "3.9"
services:
  worker:
    build: .
    environment:
      API_ID: $API_ID  # defaults to None
      API_HASH: $API_HASH  # defaults to None
      BOT_TOKEN: $BOT_TOKEN
      OWNER_ID: $OWNER_ID
      REDIS_URI: $REDIS_URI
      REDIS_PASS: $REDIS_PASS 
